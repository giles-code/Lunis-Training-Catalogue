name: Validate and publish course catalogue

on:
  push:
    branches:
      - main
      - claude/coding-help-lsmkts
    paths-ignore:
      - dist/catalogue.json
  pull_request:

permissions:
  contents: write

jobs:
  validate-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run validate
      - run: npm run build:catalogue
      - name: Commit updated website feed
        if: github.event_name == 'push'
        run: |
          if [ -z "$(git status --porcelain -- dist/catalogue.json)" ]; then
            echo "Catalogue feed is already current."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/catalogue.json
          git commit -m "chore: rebuild catalogue feed [skip ci]"
          git push
